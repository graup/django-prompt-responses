============
Installation
============

At the command line::

    $ easy_install django-prompt-responses

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-prompt-responses
    $ pip install django-prompt-responses
